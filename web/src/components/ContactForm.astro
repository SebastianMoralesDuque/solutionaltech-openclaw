---
---
<section id="contacto" class="max-w-4xl mx-auto mb-40">
    <div class="bg-gradient-to-br from-slate-900 to-slate-950 p-8 md:p-12 rounded-3xl border border-slate-800 shadow-2xl">
        <div class="text-center mb-10 space-y-4">
            <h2 class="text-4xl font-bold text-white">¿Listo para automatizar?</h2>
            <p class="text-slate-400">Cuéntanos sobre tu negocio y un experto en IA se pondrá en contacto contigo.</p>
        </div>

        <form id="contact-form" class="space-y-6">
            <div class="grid md:grid-cols-2 gap-6">
                <div class="space-y-2">
                    <label for="name" class="text-sm font-medium text-slate-300 ml-1">Nombre</label>
                    <input 
                        type="text" 
                        id="name" 
                        name="name" 
                        placeholder="Tu nombre"
                        required
                        class="w-full bg-slate-800/50 border border-slate-700 rounded-xl px-4 py-3 text-white placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500 transition-all"
                    />
                </div>
                <div class="space-y-2">
                    <label for="email" class="text-sm font-medium text-slate-300 ml-1">Email corporativo</label>
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        placeholder="tu@empresa.com"
                        required
                        class="w-full bg-slate-800/50 border border-slate-700 rounded-xl px-4 py-3 text-white placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500 transition-all"
                    />
                </div>
            </div>

            <div class="space-y-2">
                <label for="company" class="text-sm font-medium text-slate-300 ml-1">Empresa</label>
                <input 
                    type="text" 
                    id="company" 
                    name="company" 
                    placeholder="Nombre de tu empresa"
                    class="w-full bg-slate-800/50 border border-slate-700 rounded-xl px-4 py-3 text-white placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500 transition-all"
                />
            </div>

            <div class="space-y-2">
                <label for="message" class="text-sm font-medium text-slate-300 ml-1">¿Qué proceso te gustaría automatizar?</label>
                <textarea 
                    id="message" 
                    name="message" 
                    rows="4"
                    placeholder="Describe brevemente tus necesidades..."
                    class="w-full bg-slate-800/50 border border-slate-700 rounded-xl px-4 py-3 text-white placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500 transition-all resize-none"
                ></textarea>
            </div>

            <button 
                type="submit" 
                id="submit-btn"
                class="w-full bg-blue-600 hover:bg-blue-500 text-white font-bold py-4 rounded-xl shadow-lg shadow-blue-900/20 transition-all transform hover:-translate-y-1 active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed"
            >
                <span id="btn-text">Enviar propuesta</span>
            </button>
            <div id="form-message" class="hidden text-center text-sm p-4 rounded-xl"></div>
        </form>

        <script>
            const form = document.getElementById('contact-form') as HTMLFormElement;
            const btn = document.getElementById('submit-btn') as HTMLButtonElement;
            const btnText = document.getElementById('btn-text');
            const formMsg = document.getElementById('form-message');

            form?.addEventListener('submit', async (e) => {
                e.preventDefault();
                
                // UI States
                btn.disabled = true;
                if (btnText) btnText.textContent = 'Enviando...';

                // Simulate API call (Frontend persona focus)
                await new Promise(resolve => setTimeout(resolve, 1500));

                if (formMsg) {
                    formMsg.classList.remove('hidden', 'bg-red-500/10', 'text-red-400', 'bg-emerald-500/10', 'text-emerald-400');
                    formMsg.classList.add('bg-emerald-500/10', 'text-emerald-400');
                    formMsg.textContent = '¡Gracias! Tu mensaje ha sido enviado con éxito. Te contactaremos pronto.';
                }

                form.reset();
                if (btnText) btnText.textContent = 'Propuesta Enviada';
                
                setTimeout(() => {
                    btn.disabled = false;
                    if (btnText) btnText.textContent = 'Enviar propuesta';
                    formMsg?.classList.add('hidden');
                }, 5000);
            });
        </script>
        
        <p class="text-center text-xs text-slate-500 mt-6 italic">
            * Te responderemos en menos de 24 horas hábiles.
        </p>
    </div>
</section>
